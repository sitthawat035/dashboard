# Browser Automation Skill Dependencies
# Install with: pip install -r requirements.txt

playwright>=1.40.0

# After installing playwright, run:
# playwright install
# This will download the browser binaries needed by Playwright.
